package com.blades.controller.admin;

import com.blades.frontend.page.common.Page;

public class AdminPage extends Page {

    protected AdminPage() {
        super(builder("admin", "admin", null));
    }
}
